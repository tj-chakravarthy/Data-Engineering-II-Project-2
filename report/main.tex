\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{url}
\usepackage{float}
\usepackage{tikz}
\usepackage[colorlinks=true,linkcolor=black,urlcolor=blue,citecolor=black]{hyperref}
\usepackage{subcaption}
\usepackage{booktabs}
\captionsetup[subfigure]{font=footnotesize}
\usetikzlibrary{positioning,arrows.meta}


\hypersetup{
    pdftitle={Data Engineering II Mini Project --- Group 16},
    pdfauthor={Group 16, Uppsala University}
}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\begin{document}

\title{Scalable Stream Processing Using Apache Pulsar}

\author{
\IEEEauthorblockN{
    Tejas Chakravarthy,
    Andreas Hadjoullis,
    Julios Fotiou,
    Tanay Singh,
    Adnan Erlangga Raharaja
}
\IEEEauthorblockA{
    Uppsala University \\
    1TD076 Data Engineering II, VT 2026, Group 16 \\
    Source code: \url{https://github.com/tj-chakravarthy/Data-Engineering-II-Project-2}
}
}

\maketitle

\begin{abstract}
This paper presents a scalable GitHub analytics system using Apache Pulsar.
The system collects repository metadata from the GitHub API, enriches it using
additional API requests, and continuously aggregates the results to answer four
analytics questions about languages, commits, unit tests, and CI/CD usage. The
system is deployed with Docker and Docker Swarm across cloud virtual machines.
A 38-hour collection processed 78,110 unique repositories. The experiments show
that runner parallelism and GitHub API token count have the strongest effect on
throughput, while worker VM count and batch size have smaller effects.
\end{abstract}

\begin{IEEEkeywords}
GitHub API, Apache Pulsar, stream processing, containerization, orchestration, Docker Swarm, data analytics
\end{IEEEkeywords}

\section{Introduction}
GitHub hosts a large number of open-source software projects and provides
repository metadata through its REST API~\cite{github_rest_api}. This metadata can be useful for
understanding software development trends, such as which programming languages
are most common, which repositories are actively maintained, and how often
projects use unit tests and continuous integration. However, these higher-level
analytics are not directly available through GitHub search. Therefore, a data
engineering pipeline is needed to collect, process, and analyze repository
metadata at scale.

This project develops a GitHub analytics system using Apache Pulsar as the
streaming framework~\cite{apache_pulsar}. The system collects repository metadata from the GitHub
API, publishes it to Pulsar topics, and processes it using worker services to
produce the required analytics. The analytics component is designed to answer
the project questions, including the most common programming languages, the
most frequently updated repositories, the languages most associated with unit
testing, and the languages most associated with both unit testing and
continuous integration.
The system also addresses practical data engineering challenges. GitHub search
results are limited, so the crawler must divide the collection process into
smaller queries and use pagination. API rate limits must also be considered
when collecting repository metadata. To make the system reproducible and easier
to deploy, the implementation uses Docker containers and automation scripts.
The main contribution of this work is the design of a cloud-based and scalable
GitHub analytics pipeline using suitable data engineering frameworks. Apache
Pulsar is used as the streaming layer, while containerization and automation
support deployment across multiple virtual machines.

\section{Related Work}
\label{sec:related-work}
GitHub is widely used as a data source in empirical software engineering and
mining software repository research. Kalliamvakou et al. show that GitHub
metadata can provide useful information about software development activity, but
also emphasize that such data must be interpreted
carefully~\cite{kalliamvakou2014promises}. This is relevant to the present
project because repository metadata is used to derive analytics about
programming languages, update activity, unit testing, and continuous
integration.

Stream processing systems are useful when data should be processed
incrementally rather than only after a complete dataset has been collected.
Blamey et al. compare stream-processing systems using throughput-oriented
benchmarks under different processing loads~\cite{blamey2019apache}. In this
project, Apache Pulsar is used as the streaming layer, following a
producer-topic-consumer model where repository records are published as
messages and processed by worker services~\cite{apache_pulsar}.

Cloud deployment and automation are also relevant because the system is
intended to run across multiple virtual machines. Armstrong et al. discuss
cloud contextualization as a way to configure virtual machines during
deployment~\cite{armstrong2015contextualization}. In this project,
containerization and automation are used to make the pipeline more reproducible
and easier to deploy across cloud resources.


\section{System Architecture}
\label{sec:architecture}

\subsection{Overview}
The proposed system is designed as a cloud-based streaming pipeline for
collecting, enriching, and analyzing GitHub repository data.
Figure~\ref{fig:architecture} shows the high-level architecture of the system.
The architecture separates the pipeline into three main stages: data collection,
repository metadata enrichment, and data aggregation. This separation allows the
runners responsible for enriching the metadata repository to be developed,
deployed, and scaled independently.

The pipeline starts with a crawler that retrieves repository metadata from the
GitHub API. Each repository is converted into a structured record and published
to a raw repository stream. A configurable number of processing runners consume
these records and enrich them with additional information using the GitHub API.
This enrichment is necessary because the initial metadata is not sufficient to
answer all project questions, especially those related to commit activity,
unit-test usage, and CI/CD usage. The runners then publish data to an
aggregation stream. Finally, the aggregator computes a new answer to each
question based on all aggregated data so far.

\begin{figure}[!t]
\centering
\begin{tikzpicture}[
    node distance=0.25cm,
    component/.style={
        rectangle,
        draw,
        rounded corners,
        align=center,
        minimum width=1.45cm,
        minimum height=0.48cm,
        font=\scriptsize
    },
    stream/.style={
        rectangle,
        draw,
        dashed,
        align=center,
        minimum width=1.45cm,
        minimum height=0.44cm,
        font=\scriptsize
    },
    arrow/.style={-{Stealth}, thick}
]

\node[component] (github) {GitHub API};
\node[component, below=of github] (crawler) {Metadata\\Crawler};
\node[stream, below=of crawler] (raw) {Raw Repository\\Stream};

\node[component, below left=0.45cm and 0.28cm of raw] (runner1) {Runner 1\\Enrichment};
\node[below=0.62cm of raw, font=\normalsize] (runnerdot) {...};
\node[component, below right=0.45cm and 0.28cm of raw] (runnern) {Runner $N$\\Enrichment};

\node[stream, below=1.20cm of raw] (processed) {Data\\Stream};
\node[component, below=of processed] (aggregator) {Aggregator};
\node[component, below=of aggregator] (results) {Live Q1-Q4\\Results};

\draw[arrow] (github) -- (crawler);
\draw[arrow] (crawler) -- (raw);
\draw[arrow] (raw) -- (runner1);
\draw[arrow] (raw) -- (runnern);
\draw[arrow] (runner1) -- (processed);
\draw[arrow] (runnern) -- (processed);
\draw[arrow] (processed) -- (aggregator);
\draw[arrow] (aggregator) -- (results);

\end{tikzpicture}
\caption{High-level architecture of the GitHub analytics system. The crawler
collects repository metadata, runners enrich the records using additional
GitHub API requests, and the aggregator continuously updates the analytics
results.}
\label{fig:architecture}
\end{figure}


\subsection{Deployment and Automation}
The system is intended to run across multiple cloud virtual machines using
containerized services. The master node will host the coordination services,
while the worker nodes will run the services responsible for data processing.
Docker is used to containerize each service, while Docker Swarm is used for
orchestrating these services across a cluster of one Manager and maximum four
Workers. This deployment model supports scalability experiments by allowing the
number of runners and the number of worker machines to be changed.

\subsection{Stream Processing with Apache Pulsar}
Apache Pulsar is used as the streaming layer between the crawler, runners, and
aggregator. The crawler acts as a producer, while the runners act as both
consumers and producers. Finally, the aggregator consumes from the runners. As a
result, the crawler does not need to know how many runners are active, and the
processing stage can be scaled independently. The streaming layer also helps
improve locality by distributing records to runners on worker nodes instead of
using a single centralized enrichment component. Each runner processes its
assigned records locally and publishes the data back to the stream. This reduces
unnecessary data movement while allowing more runners or worker machines to be
added without changing the crawler or aggregator.


\subsection{Data Collection}
The crawler is responsible for collecting repository metadata from the GitHub
API. Since the GitHub search interface limits the number of results returned by
a single query, the crawler divides the collection process into smaller queries
and uses pagination. Crawling can take a significant amount of time,
particularly for large datasets, and the GitHub API also imposes request limits.
Re-crawling data for days that have already been collected would therefore be an
inefficient use of resources. For this reason, collected results are cached on
disk for each day and reused across runs whenever data for that day is
needed again. The crawler also removes duplicate repositories before publishing
records to the stream. This ensures that each repository is processed only once
by the downstream stages.

The collected metadata provides the initial information needed by the pipeline.
Some analytics can be derived directly from this metadata, while others require
additional enrichment through the GitHub API. Therefore, the crawler only performs
the initial collection step, and the runners are responsible for gathering any
extra information needed by the later analytics stage.


\subsection{Enrichment and Aggregation}
The runners enrich repository records by retrieving the additional information
needed for the analytics questions. Commit activity is estimated by querying
the GitHub commits endpoint, while unit-test and CI/CD indicators are detected
by checking selected repository paths and configuration files. This avoids
downloading the full repository while still providing the extra information
needed for the analytics stage.

To reduce communication overhead, runners can send the data in batches of
configurable batch size. The aggregator consumes these data and re-evaluates the
current global answers to the four project questions. Since aggregation is
performed continuously, the system produces live results that are updated as
more repositories are processed.

The analytics stage also supports adaptive top-$N$ queries. The runners extract
general repository features, while the aggregator computes the final rankings.
The value of $N$ is provided as a runtime configuration parameter, through the
deployment environment file. Therefore, changing from top-10 to top-20 only
requires updating the configuration and restarting the aggregator component,
without changing the crawler, runners, or streaming layer.

\section{Experiments and Results}
\label{sec:experiments}

\subsection{GitHub Analytics Results}
The application collected data for 38 hours, during which a total amount of
78,110 unique repositories was processed and taken into account for the four
project questions. Of those 78,110 repositories, 36,185 were taken from the
(crawler) cache of a previous run.

\subsubsection{Q1: Top Languages by Repository Count}
Figure~\ref{fig:q1a} shows the top 10 languages by repository count. The largest
group is Unknown (26,162), representing repositories with no detected primary
language. Among named languages, HTML (14,367), Python (9,421), JavaScript
(7,858), and TypeScript (6,163) lead, showing the dominance of web-facing
languages in the collected dataset.

\subsubsection{Q2: Most Frequently Updated Repositories}
Figure~\ref{fig:q1b} shows the top 10 repositories by commit count. The leading
entries are mainly kernel forks and mirrors, with the top two
exceeding 800,000 commits. This shows that raw commit count
must be interpreted carefully, since mirrored repositories can
appear disproportionately active.

\begin{figure}[!t]
    \centering

    \begin{subfigure}{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/q1_languages.png}
        \caption{Top 10 programming languages by repository count.}
        \label{fig:q1a}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/q2_commits.png}
        \caption{Top 10 repositories by total commit count.}
        \label{fig:q1b}
    \end{subfigure}

    \caption{Repository language and commit count distributions ($n=78{,}110$).}
    \label{fig:q1}
\end{figure}

\subsubsection{Q3: Languages with Test-Driven Development}
Figure~\ref{fig:q3a} shows the top 10 languages by repositories containing test
files. Python leads with 1,293 repositories, followed by
TypeScript (680) and JavaScript (595). Overall, the results
suggest that test adoption is relatively low compared with the
total number of processed repositories.

\subsubsection{Q4: Languages with TDD and CI/DevOps}
Figure~\ref{fig:q3b} shows languages with both test files and CI/CD
configuration. Python (416), TypeScript (214), and JavaScript (185) lead again.
However, only part of the repositories with test indicators also contain CI/CD
configuration, showing that testing and CI/CD adoption do not always appear
together.

\begin{figure}[!t]
    \centering

    \begin{subfigure}{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/q3_tdd.png}
        \caption{Top 10 languages by TDD adoption.}
        \label{fig:q3a}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/q4_tdd_ci.png}
        \caption{Top 10 languages by combined TDD and CI/DevOps adoption.}
        \label{fig:q3b}
    \end{subfigure}

    \caption{TDD and combined TDD--CI/DevOps adoption across programming languages ($n=78{,}110$).}
    \label{fig:q3}
\end{figure}



\subsubsection{Live Results Stability}
Figures~\ref{fig:trend_q1} and~\ref{fig:trend_q34} show that the
rankings stabilise early during processing. Q1 stabilises almost
immediately, with the leading languages keeping fixed positions
for the rest of the dataset. Q2 also converges early among the
top repositories, although some lower-ranked entries shift later.
For Q3 and Q4, Python, TypeScript, and JavaScript remain the
dominant languages after brief initial fluctuations.

\begin{figure}[t]
    \centering

    \begin{subfigure}{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/trend_q1_rank_stability.png}
        \caption{Q1 language ranks stabilise within the first 1,000 repositories.}
        \label{fig:trend_q1a}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/trend_q2_rank_stability.png}
        \caption{Q2 language ranks stabilise within the first 1,000 repositories.}
        \label{fig:trend_q1b}
    \end{subfigure}

    \caption{Language-rank stability trends for Q1 and Q2.}
    \label{fig:trend_q1}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/trend_q3_q4_rank_stability.png}
    \caption{Language-rank stability over time for Q3 (left) and Q4 (right).}
    \label{fig:trend_q34}
\end{figure}

\subsection{Experiments}
\subsubsection{Experimental Setup}

The experiments evaluate the scalability and performance of the GitHub
analytics pipeline under different deployment configurations. Each run was
executed for 30 minutes. The main metrics were the total number of processed
repositories, throughput in repositories per minute, and average end-to-end
latency in seconds. Latency was measured as the time between a repository being
emitted by the crawler and the corresponding result being received by the
aggregator.

The evaluation followed a staged design instead of a full grid search. Each
experiment group varied one parameter while keeping the others fixed. The
tested parameters were the number of analytics runners, the number of GitHub
API tokens, the number of worker VMs, and the batch size used when runners
send results to the aggregator.

\begin{table}[!t]
\centering
\caption{Scalability and batching experiments across runners, GitHub API tokens, worker VMs, and batch sizes.}
\label{tab:scaling-experiments}
\begin{tabular}{llrrr}
\toprule
Experiment & Scale & Repos & Throughput & Latency \\
\midrule
Runner & 1 runner  & 700  & 23.33 & 871.94 \\
Runner & 2 runners & 1520 & 50.67 & 561.43 \\
Runner & 4 runners & 1960 & 65.33 & 291.74 \\
Runner & 8 runners & 1920 & 64.00 & 135.29 \\
\midrule
GitHub API token & 1 token  & 380  & 12.67 & 136.31 \\
GitHub API token & 3 tokens & 1200 & 40.00 & 261.60 \\
GitHub API token & 5 tokens & 1980 & 66.00 & 279.41 \\
\midrule
Worker VM & 1 worker  & 1980 & 66.00 & 323.93 \\
Worker VM & 2 workers & 2000 & 66.67 & 338.28 \\
Worker VM & 4 workers & 1980 & 66.00 & 310.87 \\
\midrule
Batch size & 1   & 2002 & 66.73 & 281.83 \\
Batch size & 20  & 1960 & 65.33 & 297.46 \\
Batch size & 100 & 1800 & 60.00 & 333.27 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.8\linewidth]{figures/all_experiments_processed_repositories.pdf}
    \caption{Processed repositories across all experiment runs.}
    \label{fig:processed-repositories}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.8\linewidth]{figures/all_experiments_throughput.pdf}
    \caption{Throughput across all experiment runs.}
    \label{fig:throughput}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.8\linewidth]{figures/all_experiments_average_latency.pdf}
    \caption{Average end-to-end latency across all experiment runs.}
    \label{fig:latency}
\end{figure}

\subsubsection{Runner Scalability}

The runner scalability experiment varied the number of runners from 1 to 8 while
keeping the number of workers, tokens, and batch size fixed. The results are
shown in Table~\ref{tab:scaling-experiments} and
Figures~\ref{fig:processed-repositories},~\ref{fig:throughput},~and~\ref{fig:latency}.

Increasing the number of runners had the strongest effect on performance.
Throughput increased from 23.33 repositories/minute with one runner to about
65 repositories/minute with four runners. Increasing the number of runners to
eight did not improve throughput further, suggesting that the system reaches a
bottleneck after four runners. This is likely caused by GitHub API access and
the limited number of available tokens. However, latency continued to decrease
with more runners, which suggests that additional runners reduce the time that
repositories spend waiting before being processed, even when total throughput
is already capped.

\subsubsection{Token Scaling}

The token scaling experiment varied the number of GitHub API tokens while
keeping four runners, four workers, and batch size 20. The results are shown in
Table~\ref{tab:scaling-experiments} and
Figures~\ref{fig:processed-repositories},~\ref{fig:throughput},~and~\ref{fig:latency}.

The number of tokens also had a clear impact on throughput. With one token, the
system processed only 380 repositories, while with five tokens it processed
1980 repositories. Throughput increased from 12.67 to 66.00
repositories/minute. This confirms that GitHub API access is a major limiting
factor. The latency results are less direct: latency increases as more tokens
are added. This does not necessarily mean that the system becomes slower per
repository. Instead, more tokens allow many more repositories to enter and stay
active in the pipeline, which can increase the measured end-to-end time for
completed records.

\subsubsection{Worker VM Scalability}

The worker scaling experiment varied the number of worker VMs while keeping four
runners, five tokens, and batch size 20. The results are shown in
Table~\ref{tab:scaling-experiments} and
Figures~\ref{fig:processed-repositories},~\ref{fig:throughput},~and~\ref{fig:latency}.

Increasing the number of worker VMs had almost no effect on throughput.
Throughput stayed close to 66 repositories/minute in all three configurations.
This suggests that the workload is not computationally expensive enough to
benefit significantly from more machines. Most of the time is likely spent
waiting for GitHub API requests rather than performing CPU-intensive local
processing. Therefore, worker VM scaling is less important than runner
parallelism and token availability for this workload.

\subsubsection{Batch Size}

The batch-size experiment varied how often runners send results to the
aggregator. The results are shown in Table~\ref{tab:scaling-experiments} and
Figures~\ref{fig:processed-repositories},~\ref{fig:throughput},~and~\ref{fig:latency}.

Batch size had only a small effect compared with runners and tokens. Smaller
batches gave slightly higher throughput and lower latency, but the difference
was not large enough to identify batching as the main bottleneck. This suggests
that communication through Pulsar and aggregation are not as expensive as the
external GitHub API enrichment work. Larger batches may delay updates because
records wait longer before being sent to the aggregator, which can explain the
higher latency for batch size 100.

\subsection{Discussion}

Overall, no experiment run was able to process more than 2000 repositories
during the 30 minutes processing time. During the enrichment phase of the
pipeline, it is possible for each repositories to require from 3 to 19 extra
calls to the GitHub API. Meaning that reaching 2000 repositories on average is
about when all GitHub tokens become drained, and no more work can be done.

\section{Conclusion}
\label{sec:conclusion}
This project built a scalable stream-processing system for GitHub analytics.
The pipeline processed 78,110 unique repositories and answered all four
analytics questions. Experiments showed that runners and GitHub API tokens
affected throughput the most, while worker VMs and batch size had limited
impact. Overall, performance was mainly constrained by GitHub API access.

% References
\bibliographystyle{IEEEtran}
\bibliography{main}

\end{document}
